# MDNotes Server

Project Goals:

- Support for multiple users


Ideas:

User Class:

  properies:

    - database (connection to the users sqlite database)
    - filesystem (class with operations for the users files)